---
title: "Quickstart"
description: "Start the Verity development environment and explore the demo."
---

## 1. Start the Web App

Verity's web layer is built with Bun and Hono.

```bash
cd packages/web
bun install
bun run dev
```

## 2. Explore the Demo Flow

The demo provides a UI-only walkthrough of the Verity experience. Visit these routes in order to see the full lifecycle:

1. **Onboarding**: `/demo/onboarding`
2. **Host Dashboard**: `/demo/host/dashboard`
3. **Create Session**: `/demo/host/create/details` → `/demo/host/create/goals`
4. **Attendee Join**: `/demo/learner/join`
5. **Session Lifecycle**: `/demo/session/live` → `/demo/session/evaluating` → `/demo/session/results`

## 3. Test CRE Workflows (Optional)

To run the Chainlink CRE workflows directly — initiation and settlement — see [Testing CRE Workflows](/cre-testing). That guide covers simulation commands, environment variables, and testing with Abi Ninja against pre-deployed contracts.

## 4. Review the Docs

- **Product Overview**: [Overview](/overview)
- **Contracts and CRE**: [Contracts and CRE](/contracts-and-cre)
- **User Journeys**: [Product and User Flow](/product-and-user-flow)

<Note>
  The demo flow currently uses mock data for product iteration. Real settlement and AI evaluation are handled by the [CRE Workflows](/contracts-and-cre#workflow-2-settlement).
</Note>
